import {
    teamApplicationStartHandler,
} from '../../../handlers/teamApplicationStartButton.js';

export default {
    name: teamApplicationStartHandler.name,
    execute: teamApplicationStartHandler.execute,
};
