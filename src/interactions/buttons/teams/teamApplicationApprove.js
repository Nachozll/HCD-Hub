import {
    teamApplicationApproveButtonHandler,
} from '../../../handlers/teamApplicationApproveButton.js';

export default {
    name:
        teamApplicationApproveButtonHandler.name,

    execute:
        teamApplicationApproveButtonHandler.execute,
};
