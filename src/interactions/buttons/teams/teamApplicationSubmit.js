import {
    teamApplicationSubmitButtonHandler,
} from '../../../handlers/teamApplicationSubmitButton.js';

export default {
    name:
        teamApplicationSubmitButtonHandler.name,

    execute:
        teamApplicationSubmitButtonHandler.execute,
};
