import {
    teamApplicationTypeHandler,
} from '../../../handlers/teamApplicationTypeButton.js';

export default {
    name: teamApplicationTypeHandler.name,
    execute: teamApplicationTypeHandler.execute,
};
