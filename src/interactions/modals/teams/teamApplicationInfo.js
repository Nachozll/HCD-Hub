import {
    teamApplicationInfoModalHandler,
} from '../../../handlers/teamApplicationInfoModal.js';

export default {
    name: teamApplicationInfoModalHandler.name,
    execute:
        teamApplicationInfoModalHandler.execute,
};
